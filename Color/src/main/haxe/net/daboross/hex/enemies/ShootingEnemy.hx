package net.daboross.hex.enemies.enemies;

import net.daboross.hex.enemies.BasicEnemy;

class ShootingEnemy extends BasicEnemy {

    public function new(space:SpaceHandler, sheet:SpriteSheet, frame:Dynamic) {
        super(space, sheet, frame);
    }
}
