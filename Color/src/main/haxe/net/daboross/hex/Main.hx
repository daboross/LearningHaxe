package net.daboross.hex;

import net.daboross.hex.SpaceHandler;

class Main {

    public static function main() {
        var space:SpaceHandler = new SpaceHandler();
        space.start();
    }
}
